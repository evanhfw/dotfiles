# ~/.config/mise/config.toml — managed by chezmoi
#
# Versi di-pin supaya identik di macOS & Linux.
#   mise ls                 → lihat yang terpasang
#   mise use -g node@latest → update versi
#   mise install            → pasang sesuai file ini

[tools]
bun  = "1.3.13"
node = "25.9.0"
pnpm = "11.1.2"
rust = "1.97.1"
uv   = "0.11.11"
